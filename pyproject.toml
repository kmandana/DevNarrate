[project]
name = "devnarrate"
version = "0.1.0"
description = "The AI that narrates your code changes, from commits to deployments."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.20.0",
    "tiktoken>=0.5.0",
]
